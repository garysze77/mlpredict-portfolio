# 延長逾時，避免慢速網路時安裝失敗
fetch-timeout=120000
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000

# 若無法連線 registry.npmjs.org，可改用鏡像（取消註解其中一行）：
# registry=https://registry.npmmirror.com
# registry=https://registry.npm.taobao.org
